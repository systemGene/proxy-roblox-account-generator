# USAGE
```
This roblox account generator works by attempting to create an account without captcha and uses proxy to try and bypass the captcha.

WARNING: This will not effectively not WORK if you have very trash or bad proxies.
I suggest you get authentic proxies.
```

# REQUIREMENTS
```
pip install requests
pip install threading
pip install colorama
pip install random_username
```
